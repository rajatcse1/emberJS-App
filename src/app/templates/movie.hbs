<div class="row">
    <div class="col-lg-2">
        <ul class="nav nav-pills nav-stacked">
            {{#each genre in model}}
            <li>
                <a href="" {{action gotoMovieListByGenre genre}}>{{genre.name}}</a>
            </li>
            {{/each}}
        </ul>
    </div>
    <div class="col-lg-10">
        {{outlet}}
    </div>
</div>